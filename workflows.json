{
  "version": "1.0",
  "workflows": {
    "dev": {
      "command": "npm run dev",
      "description": "Start the Express server and Vite development environment",
      "env": {},
      "restartPolicy": "always"
    }
  }
}