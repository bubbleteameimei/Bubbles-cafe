{
  "version": "1.0",
  "workflows": {
    "server": {
      "command": "npm run dev",
      "description": "Start the application server",
      "env": {
        "NODE_ENV": "development"
      },
      "restartPolicy": "always"
    }
  }
}