{
  "version": "1.0",
  "workflows": {
    "Start application": {
      "command": "npm run dev",
      "description": "Start the Express server and Vite development environment",
      "env": {},
      "restartPolicy": "always"
    }
  }
}