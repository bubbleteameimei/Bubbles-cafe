- [Bubbles Cafe backend auth](bubbles-cafe-auth.md) — Supabase JWT verification via service role `auth.getUser(token)` with in-memory token cache; `verifyAuthToken` accepts both local JWT and Supabase JWTs.
- [Bubbles Cafe DB migration](bubbles-cafe-db.md) — Neon DB needs manual SQL migration; drizzle-kit binary not reliably in PATH; run SQL files via tsx script against pool. Posts table needed `baseline_likes/baseline_dislikes` added; users `isAdmin` maps to `is_admin` column.
- [Bubbles Cafe CSRF](bubbles-cafe-csrf.md) — CSRF middleware was blocking all mutations; removed since app uses JWT Bearer which is CSRF-immune. Frontend sends Supabase JWT as `Authorization: Bearer` via `attachAuthHeader`.
